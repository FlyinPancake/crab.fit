on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  container:
    uses: ./.github/workflows/build_container.yml
    permissions:
      contents: write
      id-token: write
      packages: write
